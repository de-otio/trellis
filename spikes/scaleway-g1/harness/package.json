{
  "name": "scaleway-g1-spike-harness",
  "version": "0.0.0",
  "private": true,
  "description": "G1 feasibility-spike test harness for the AWS to Scaleway portability check. Uses plain AWS SDK v3 clients against Scaleway's compat surfaces (Object Storage, MNQ SQS) plus node-postgres — it deliberately does NOT use Trellis code. It tests Scaleway's API compatibility, not our ports.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check-postgres": "tsx check-postgres.ts",
    "check-s3-presigned": "tsx check-s3-presigned.ts",
    "check-sqs": "tsx check-sqs.ts",
    "check-container": "tsx check-container.ts",
    "run-all": "tsx run-all.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1090.0",
    "@aws-sdk/client-sqs": "^3.1090.0",
    "@aws-sdk/s3-presigned-post": "^3.1090.0",
    "@aws-sdk/s3-request-presigner": "^3.1090.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
